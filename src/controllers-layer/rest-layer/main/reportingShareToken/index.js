const express = require("express");

// ReportingShareToken Db Object Rest Api Router
const reportingShareTokenRouter = express.Router();

// add ReportingShareToken controllers

module.exports = reportingShareTokenRouter;
